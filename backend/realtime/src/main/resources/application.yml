spring:
  application:
    name: routiqo-realtime
  threads:
    virtual:
      enabled: true
server:
  port: 8082
  address: ${SERVER_ADDRESS:127.0.0.1}
  error:
    include-message: never
    include-stacktrace: never
    include-binding-errors: never

