server:
  # Rate limits key on the socket peer, or on X-Forwarded-For only from ROUTIQO_TRUSTED_PROXY_CIDRS
  # (ClientAddressResolver, ADR 0074). Scheme and host forwarding stay off.
  forward-headers-strategy: none
