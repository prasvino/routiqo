server:
  # Rate limits never trust client-supplied forwarding headers; only hops from
  # ROUTIQO_TRUSTED_PROXY_CIDRS are read (ClientAddressResolver, ADR 0074). Scheme and host forwarding stay off.
  forward-headers-strategy: none
