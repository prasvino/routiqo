# Working method
Read AGENTS and relevant documents. Define a focused feature spec, implement the smallest coherent behavior, test, inspect rendered UI, review diffs, update contracts/ADRs and report evidence.
Pause only the section needing missing input and continue independent work. Keep secrets and provider credentials out of task outputs.

