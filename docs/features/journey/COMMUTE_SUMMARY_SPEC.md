# Confirmed commute summaries — local first slice

Summarize only confirmed completed COMMUTE records from the verified account's bounded journey snapshot partition. Trips, active journeys and pending commands are excluded. This is an on-demand read-only view: no new persistence, server endpoint, provider call, location collection or automatic journal.

Group by the start instant's calendar month in the browser's resolved time zone. Display the zone and label the result as based on confirmed records saved on this device; it may omit older journeys or records from other devices. Do not imply complete account history. Sort newest months first. Count completed journeys and sum their exact elapsed microseconds before flooring the total to minutes; elapsed time uses UTC instants even across daylight-saving transitions. Describe it as recorded elapsed time, not measured driving time. No invented distance, route updates, saved places, efficiency or environmental metrics.

Validate the account-bound snapshot through the existing strict validator (100 entries/64KiB), reject duplicate/corrupt/wrong-account inputs, and reject invalid time zones. Never partially summarize invalid records. The UI fails with a generic unavailable message and does not expose another account's content; mount only in the verified journey workspace. Empty state explains that summaries appear after a commute is completed and confirmed.

Use a quiet disclosure section with month headings, counts and elapsed time, existing typography and no charts or extra cards. The disclosure keeps active journey controls primary. No new animation. Tests cover UTC/local month boundaries, DST elapsed duration, sub-minute accumulation, exclusion of unconfirmed trips/active records, account mismatch, duplicates and invalid zones. Feature threats: T02 owner isolation, T12 timing/history privacy, T14 bounded work, T19 no new retained copies. Real browser/accessibility and native parity remain release verification work; root owns follow-up.
