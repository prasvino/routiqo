# Native planning backup text

Extend the existing shared backup format to native Profile without adding file-system permissions or cloud services. Export freshly reads validated SQLite data and shows selectable JSON; only an explicit Share action opens the operating-system text share sheet. Disclose that route names, dates and notes are unencrypted. Android share completion does not prove delivery; never claim the backup was saved.

Restore accepts pasted JSON up to the shared 512 KB bound, validates before preview and requires explicit Restore. Re-read and merge inside the existing exclusive SQLite transaction. Keep current edits on ID collisions, enforce 100-record limits, and change nothing on failure. Close discards pasted/exported text. Server credentials and journey outbox records are excluded.

Visual thesis: calm cream surface, green typography, generous touch targets. Content: short scope notice, export/restore actions, one text area and review counts, explicit confirmation. Interaction: native slide modal, keyboard-aware scrolling, no decorative motion. Text sizing follows native accessibility settings.

Verify shared parser/merge tests, native strict types and Android export. Native share delivery, paste limits, keyboard, screen reader, SQLite restart and large-text behavior still require a real Android device/emulator; do not claim that an export build verifies those behaviors. File picker/document export is a later enhancement; this slice explicitly provides text backup.
